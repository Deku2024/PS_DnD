// Placeholder unit spec recreated to satisfy build references.
// The real unit tests use Angular TestBed and live in auth.spec.ts

describe('placeholder', () => {
  it('placeholder test', () => {
    expect(true).toBeTruthy();
  });
});
