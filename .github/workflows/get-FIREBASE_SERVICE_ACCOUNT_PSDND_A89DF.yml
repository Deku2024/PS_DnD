name: Get-FIREBASE_SERVICE_ACCOUNT_PSDND_A89DF
on: workflow_dispatch
jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ secrets.FIREBASE_SERVICE_ACCOUNT_PSDND_A89DF }}" > secret.txt
      - uses: actions/upload-artifact@v4
        with:
          name: secret-FIREBASE_SERVICE_ACCOUNT_PSDND_A89DF
          path: secret.txt
